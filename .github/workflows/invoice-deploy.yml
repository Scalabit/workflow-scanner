name: Deploy Invoice Service

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  deploy-invoice:
    runs-on: ubuntu-latest  
    
    defaults:
      run:
        working-directory: invoice/terraform
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v4
      with:
        terraform_version: "1.5.0"

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v3
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v3

    - name: Terraform Init
      run: terraform init

    - name: Import existing resources if they exist
      run: |
        terraform import \
          -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
          -var="email_username=${{ secrets.EMAIL_USERNAME }}" \
          -var="email_password=${{ secrets.EMAIL_PASSWORD }}" \
          -var="imap_server=${{ secrets.IMAP_SERVER }}" \
          -var="smtp_server=${{ secrets.SMTP_SERVER }}" \
          google_compute_firewall.phi_firewall allow-phi-api || true
        
        terraform import \
          -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
          -var="email_username=${{ secrets.EMAIL_USERNAME }}" \
          -var="email_password=${{ secrets.EMAIL_PASSWORD }}" \
          -var="imap_server=${{ secrets.IMAP_SERVER }}" \
          -var="smtp_server=${{ secrets.SMTP_SERVER }}" \
          google_compute_instance.phi_instance projects/${{ secrets.GCP_PROJECT_ID }}/zones/us-central1-a/instances/phi35-invoice-processor || true

    - name: Terraform Plan
      run: |
        terraform plan \
          -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
          -var="email_username=${{ secrets.EMAIL_USERNAME }}" \
          -var="email_password=${{ secrets.EMAIL_PASSWORD }}" \
          -var="imap_server=${{ secrets.IMAP_SERVER }}" \
          -var="smtp_server=${{ secrets.SMTP_SERVER }}" \
          -out=tfplan

    - name: Destroy existing instance
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      run: |
        terraform destroy -auto-approve \
          -target=google_compute_instance.phi_instance \
          -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
          -var="email_username=${{ secrets.EMAIL_USERNAME }}" \
          -var="email_password=${{ secrets.EMAIL_PASSWORD }}" \
          -var="imap_server=${{ secrets.IMAP_SERVER }}" \
          -var="smtp_server=${{ secrets.SMTP_SERVER }}" || true

    - name: Terraform Plan (after destroy)
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      run: |
        terraform plan \
          -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
          -var="email_username=${{ secrets.EMAIL_USERNAME }}" \
          -var="email_password=${{ secrets.EMAIL_PASSWORD }}" \
          -var="imap_server=${{ secrets.IMAP_SERVER }}" \
          -var="smtp_server=${{ secrets.SMTP_SERVER }}" \
          -out=tfplan-new

    - name: Terraform Apply
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      run: terraform apply -auto-approve tfplan-new

    - name: Get Instance IP
      if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
      run: |
        INSTANCE_IP=$(gcloud compute instances describe phi35-invoice-processor --zone=us-central1-a --format='get(networkInterfaces[0].accessConfigs[0].natIP)')
        echo "Invoice service deployed at: http://$INSTANCE_IP:8000"
        echo "Health check: http://$INSTANCE_IP:8000/health"